task groovy << {}
println "Hello World"
class JavaClass
{
   public void greet() {
      System.out.println("Hello from Java");
    }
}
JavaClass jc = new JavaClass();
jc.greet();
// gradle is an optionally typed language. You can choose not to specify the type of the variable
// You can declare variable with def key word

def func (x, y) {
  return x+ y // return statement is optional in  groovy
// The above expr can alternatively be written as x+y
}

def foo = 1.3
println "Value of foo is $foo"
println "Class of variable is ${foo.class}"
println "func test ${func(3,5)}"

// You can omit parenthesis while invoking the function having one or more arguments

println "func test without parentheses ${func 3, 5}"
